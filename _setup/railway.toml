[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npx tsc"

[deploy]
startCommand = "npx prisma db push && node dist/app.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
