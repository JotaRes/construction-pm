services:
  - type: web
    name: restrepoacosta
    runtime: node
    rootDir: backend
    autoDeploy: yes
    # Build phase — desde rootDir (backend/)
    #   1) Instala deps backend (incluye devDeps de TS) — trigger v2
    #   2) Instala deps frontend FORZANDO devDeps (Vite, Tailwind, PostCSS)
    #   3) Compila el frontend y copia el bundle a backend/public/
    #   4) Switches provider Prisma a postgresql para producción
    buildCommand: |
      set -e
      echo "=== [1/3] Backend deps ==="
      npm ci --include=dev
      echo "=== [2/3] Frontend build ==="
      cd ../frontend
      npm ci --include=dev
      npm run build
      echo "=== [3/3] Copy bundle to backend/public ==="
      cd ../backend
      rm -rf public
      mkdir -p public
      cp -R ../frontend/dist/. public/
      ls -la public/
      echo "=== Build complete ==="
    startCommand: |
      set -e
      echo "=== Prisma generate ==="
      npx prisma generate
      echo "=== Prisma DB push ==="
      npx prisma db push --accept-data-loss
      echo "=== Migrar Notas a Tareas (one-time, idempotente, best-effort) ==="
      npx tsx scripts/migrate-notes-to-tasks.ts || echo "migracion notas: sin cambios o ya aplicada"
      echo "=== Starting app ==="
      exec npx tsx src/app.ts
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        value: construction-pm-secret-2026
      - key: APP_PASSWORD
        value: 18418598
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
